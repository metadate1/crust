[package]
name = "crust-sim"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license-file.workspace = true

[features]
default = []
# Read-only live-object snapshots used by the separately built browser test
# harness. Production builds do not compile this introspection surface.
browser-test-harness = []

[dependencies]
crust-formats.workspace = true

[dev-dependencies]
proptest.workspace = true
serde_json.workspace = true

[lints]
workspace = true
